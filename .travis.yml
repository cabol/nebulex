language: elixir
elixir:
  - 1.4.0
  - 1.3.2
otp_release:
  - 19.2
  - 18.3
sudo: false
env:
  - MIX_ENV=test
before_script:
  - epmd -daemon
  - mix deps.get --only test
script:
  - mix coveralls.travis
after_script:
  - mix deps.get --only docs
  - MIX_ENV=docs mix inch.report
